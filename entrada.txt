VARIABLES
j:NUM;
i,max,min:NUM;
s:SEQ(NUM);
r,j:SEQ(LOG);
PROCEDIMIENTO
MAYOR(SEQ s, NUM i, NUM max)

INSTRUCCIONES

s = [3,4,2,7,9,0,11];
x,y = 1+8,x+2;


MIENTRAS (i>=j) HACER
    SI (s[i]>max && j<i) ENTONCES
            max=s[i];
            RUPTURA;
    FSI
            RUPTURA;
            i=i+2;

FMIENTRAS
