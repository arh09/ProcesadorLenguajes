PROGRAMA
VARIABLES
z:NUM;
SUBPROGRAMAS

FUNCION mayor(NUM x) dev (NUM r)
VARIABLES
j:NUM;
    INSTRUCCIONES
        si (x>5) entonces
        max,min = s[z],s[x];
            dev T;
            ruptura;
        sino
            x,y=2,4;
            mostrar(z);
            dev F;
            x=mayor(6);
        fsi
FFUNCION

INSTRUCCIONES
